type: jmx

observation:
  - name: jenkins-http-active-requests
    metricNamespace: jenkins
    objectName: io.jenkins:type=Http,name=ActiveRequests

    metric:
      - name: http.requests.active
        source: Count
        type: long_gauge
        label: http active requests
        description: The number of currently active requests against the Jenkins master Web UI
