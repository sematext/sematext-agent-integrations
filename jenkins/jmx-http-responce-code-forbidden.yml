type: jmx

observation:
  - name: jenkins-http-codes-forbidden
    metricNamespace: jenkins
    objectName: io.jenkins:type=Http,class=ResponseCodes,name=Forbidden

    metric:
      - name: http.responce.code.forbidden.count
        source: Count
        type: counter
        label: jenkins http responce code forbidden count
