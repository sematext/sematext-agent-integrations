type: jmx

observation:
  - name: jenkins-http-codes-other
    metricNamespace: jenkins
    objectName: io.jenkins:type=Http,class=ResponseCodes,name=Other

    metric:
      - name: jenkins.http.responce.code.other.count
        source: Count
        type: counter
        label: jenkins http other responce code count
