type: jmx

observation:
  - name: jenkins-http-codes-no-content
    metricNamespace: jenkins
    objectName: io.jenkins:type=Http,class=ResponseCodes,name=NoContent

    metric:
      - name: http.response.code.no.content.count
        source: Count
        type: counter
        label: http response code no content count
