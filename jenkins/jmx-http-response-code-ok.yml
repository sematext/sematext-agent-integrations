type: jmx

observation:
  - name: jenkins-http-codes-ok
    metricNamespace: jenkins
    objectName: io.jenkins:type=Http,class=ResponseCodes,name=Ok

    metric:
      - name: http.response.code.ok.count
        source: Count
        type: counter
        label: jenkins http response code ok count
