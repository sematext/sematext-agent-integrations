type: jmx

observation:
  - name: jenkins-http-codes-server-error
    metricNamespace: jenkins
    objectName: io.jenkins:type=Http,class=ResponseCodes,name=ServerError

    metric:
      - name: http.response.code.server.error.count
        source: Count
        type: counter
        label: jenkins http response code server error count
