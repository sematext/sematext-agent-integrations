type: jmx

observation:
  - name: jenkins-job-blocked
    metricNamespace: jenkins
    objectName: io.jenkins:type=Job,name=Blocked

    metric:
      - name: jobs.blocked.count
        source: Count
        type: counter
        send: false

      - name: jobs.blocked.mean.time
        source: Mean
        type: double_gauge
        send: false

      - name: jobs.blocked.time
        source: func:DoubleMultiply(jobs.blocked.count, jobs.blocked.mean.time)
        type: double_counter
        label: jenkins blocked jobs time
        unit: ms
