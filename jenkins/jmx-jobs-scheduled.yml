type: jmx

observation:
  - name: jenkins-job-scheduled
    metricNamespace: jenkins
    objectName: io.jenkins:type=Job,name=Scheduled

    metric:
      - name: jobs.scheduled
        source: Count
        type: counter
        label: jobs scheduled count
