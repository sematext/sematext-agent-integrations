type: jmx

observation:
  - name: jenkins-job-waiting
    metricNamespace: jenkins
    objectName: io.jenkins:type=Job,name=Waiting

    metric:
      - name: jobs.waiting.count
        source: Count
        type: counter
        label: jobs waiting count

      - name: jobs.waiting.mean.time
        source: Mean
        type: double_gauge
        send: false

      - name: jobs.waiting.time
        source: func:DoubleMultiply(jobs.waiting.count, jobs.waiting.mean.time)
        type: double_counter
        label: waiting jobs time
        unit: ms
