type: jmx

observation:
  - name: jenkins-nodes-offline
    metricNamespace: jenkins
    objectName: io.jenkins:type=Node,name=Offline

    metric:
      - name: nodes.offline.count
        source: Value
        type: long_gauge
        label: nodes offline count
