type: jmx

observation:
  - name: jenkins-plugins-active
    metricNamespace: jenkins
    objectName: io.jenkins:type=Plugins,name=Active

    metric:
      - name: jenkins.plugins.active.count
        source: Value
        type: long_gauge
        label: jenkins plugins active count
