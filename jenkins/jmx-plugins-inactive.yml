type: jmx

observation:
  - name: jenkins-plugins-inactive
    metricNamespace: jenkins
    objectName: io.jenkins:type=Plugins,name=Inactive

    metric:
      - name: jenkins.plugins.inactive.count
        source: Value
        type: long_gauge
        label: jenkins plugins inactive count
