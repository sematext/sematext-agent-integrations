type: jmx

observation:
  - name: jenkins-plugins-with-update
    metricNamespace: jenkins
    objectName: io.jenkins:type=Plugins,name=WithUpdate

    metric:
      - name: jenkins.plugins.with.update.count
        source: Count
        type: counter
        label: jenkins plugins with update count
