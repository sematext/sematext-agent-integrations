type: jmx

observation:
  - name: jenkins-queue-blocked
    metricNamespace: jenkins
    objectName: io.jenkins:type=Queue,name=Blocked

    metric:
      - name: queue.blocked.count
        source: Value
        type: long_gauge
        label: jenkins queue blocked count
