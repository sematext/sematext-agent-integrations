type: jmx

observation:
  - name: jenkins-queue-buildable
    metricNamespace: jenkins
    objectName: io.jenkins:type=Queue,name=Buildable

    metric:
      - name: queue.buildable.count
        source: Value
        type: long_gauge
        label: queue buildable count
