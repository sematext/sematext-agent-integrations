type: jmx

observation:
  - name: jenkins-queue-pending
    metricNamespace: jenkins
    objectName: io.jenkins:type=Queue,name=Pending

    metric:
      - name: queue.pending.count
        source: Value
        type: long_gauge
        label: jenkins queue pending count
