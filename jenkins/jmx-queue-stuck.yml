type: jmx

observation:
  - name: jenkins-queue-stuck-count
    metricNamespace: jenkins
    objectName: io.jenkins:type=Queue,name=Stuck

    metric:
      - name: queue.stuck.count
        source: Value
        type: long_gauge
        label: jenkins queue stuck count
