type: jmx

observation:
  - name: jenkins-runs-aborted-count
    metricNamespace: jenkins
    objectName: io.jenkins:type=Runs,name=Aborted

    metric:
      - name: runs.aborted
        source: Count
        type: counter
        label: runs aborted count
