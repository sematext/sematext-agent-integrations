type: jmx

observation:
  - name: jenkins-runs-not-built-count
    metricNamespace: jenkins
    objectName: io.jenkins:type=Runs,name=Not_built

    metric:
      - name: runs.not.built.count
        source: Count
        type: counter
        label: runs not built count
