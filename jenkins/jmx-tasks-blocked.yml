type: jmx

observation:
  - name: jenkins-task-blocked
    metricNamespace: jenkins
    objectName: io.jenkins:type=Task,name=Blocked

    metric:
      - name: tasks.blocked.count
        source: Count
        type: counter
        send: false

      - name: tasks.blocked.mean.time
        source: Mean
        type: double_gauge
        send: false

      - name: tasks.blocked.time
        source: func:DoubleMultiply(tasks.blocked.count, tasks.blocked.mean.time)
        type: double_counter
        label: jenkins blocked tasks time
        unit: ms
