type: jmx

observation:
  - name: jenkins-task-execution
    metricNamespace: jenkins
    objectName: io.jenkins:type=Task,name=Execution

    metric:
      - name: tasks.execution.count
        source: Count
        type: counter
        label: jenkins tasks execution count

      - name: tasks.execution.mean.time
        source: Mean
        type: double_gauge
        send: false

      - name: tasks.execution.time
        source: func:DoubleMultiply(tasks.execution.count, tasks.execution.mean.time)
        type: double_counter
        label: jenkins execution tasks time
        unit: ms
