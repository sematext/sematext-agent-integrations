type: jmx

observation:
  - name: jenkins-task-scheduled
    metricNamespace: jenkins
    objectName: io.jenkins:type=Task,name=Scheduled

    metric:
      - name: tasks.scheduled
        source: Count
        type: counter
        label: tasks scheduled count
