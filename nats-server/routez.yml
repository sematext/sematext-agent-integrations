type: json
data:
  url: /routez
  server: ${NATS_SERVER_HOSTPORT}

observation:
  - name: routez
    metricNamespace: nats_server
    path: $.

    metric:
      - name: routes
        source: num_routes
        type: long_gauge
        label: routes
        description: The number of routes

    tag:
      - name: nats.servers.server
        value: json:/routez $.server_id
