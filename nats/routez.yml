type: json
data:
  url: /routez
  server: ${NATS_HOSTPORT}

observation:
  - name: connz
    metricNamespace: nats
    path: $.

    metric:
      - name: nroutes
        source: num_routes
        type: long_gauge
        label: routes
        description: number of routes

    tag:
      - name: nats.server
        value: json:/routez $.server_id
