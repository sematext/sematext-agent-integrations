type: json
data:
  url: /routez
  server: ${NATS_HOSTPORT}

observation:
  - name: routez
    metricNamespace: nats
    path: $.

    metric:
      - name: routes
        source: num_routes
        type: long_gauge
        label: routes
        description: The number of routes

    tag:
      - name: nats.server
        value: json:/routez $.server_id
